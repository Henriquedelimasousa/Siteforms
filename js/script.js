

var fnome = document.getElementById('fnome');

function enviar() {

    if (fnome.value != "") {
        alert('Obrigado sr(a) ' + fnome.value + ' os seus dados foram encaminhados com sucesso');
    }
}